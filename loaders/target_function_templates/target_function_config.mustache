<template>
    <div class="config-root">
    {{#items}}
        {{{html}}}
    {{/items}}
    </div>
</template>

<script>
export const targetFunctionName = "{{targetFunctionName}}"
export default {
    name: "{{targetFunctionName}}.tfcfg",
    props: ["value"],
    methods: {
        handleChangeValue(name, value) {
            let temp = Object.assign({}, this.value.{{targetFunctionName}})
            temp[name] = value
            this.$emit("input", {
                "{{targetFunctionName}}": temp
            })
        },
    },
    getDefaultConfig() {
        const defaultConfig = {
            "{{targetFunctionName}}": {
            {{#items}}
                {{name}}: {{default}},
            {{/items}}
            }
        }
        return defaultConfig
    }
}
</script>

<style lang="scss" scoped>
.config-root {
    .config-title {
        font-size: 12px;
        color: #666666;
        margin: 0 0 12px 0;
    }
}
</style>