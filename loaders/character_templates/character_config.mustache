<template>
    <div class="config-root">
    {{#items}}
        {{{html}}}
    {{/items}}
    </div>
</template>

<script>
export const characterName = "{{characterName}}"
export default {
    name: "{{characterName}}.ccfg",
    props: ["value"],
    methods: {
        handleChangeValue(name, value) {
            let temp = Object.assign({}, this.value.{{characterName}})
            temp[name] = value
            this.$emit("input", {
                "{{characterName}}": temp
            })
        },
    },
    getDefaultConfig() {
        const defaultConfig = {
            "{{characterName}}": {
            {{#items}}
                {{name}}: {{default}},
            {{/items}}
            }
        }
        return defaultConfig
    }
}
</script>

<style lang="scss" scoped>
.config-root {
    .config-title {
        font-size: 12px;
        color: #666666;
        margin: 0 0 8px 0;
    }
}
</style>