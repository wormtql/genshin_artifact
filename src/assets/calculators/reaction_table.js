import { getAttribute } from "@util/attribute";
import { damageNormal } from "../../utils";


// 感电，超导，超载，扩散
export function thunder(artifacts, configObject, enemy) {
    // electro charged
    let rr = enemy.getRR("thunder");
    
}