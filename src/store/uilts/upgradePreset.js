function upgradeFrom1(preset) {
    let ret = {
        character: {},
        weapon: {},
        targetFunc: {},
        
    }
}

export default function (preset) {
    let version = preset.version ?? "1";

    switch (version) {
        case "1":

    }
}